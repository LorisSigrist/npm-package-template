//Test if the script runs
console.log("Hello Workd")

//Type Declaraton to test if types are built
type testType = number | null; 

//Export something, to check if the test can import it
export const test :testType = 5;
